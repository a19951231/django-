sass语法：
css不是一门编程语言，他无法像js和python那样拥有逻辑处理能力，以及导入其他css文件中的样式都做不到，而sass就是为了
解决css的这些问题，他允许你使用变量，嵌套规则，mixins，导入等众多功能，并且完全兼容css语法，sass文件不能直接被网页
所识别，写完sass后，还需要专门的工具转化为css才能使用

sass文件后缀名：
sass文件有2种后缀名，一个是scss，一个是sass，不同的后缀名，相应的语法也不一样，这里我们使用scss的后缀名，包括后面
讲到的sass语法，也就是scss的后缀名的语法，如：scss的后缀名写的代码需要花括号，而sass写的代码是不需要花括号，这就是
scss与sass区别

使用gulp将sass转换为css：
将sass文件转换为css文件的工具有很多，这里我们就使用之前讲过的gulp来实现，这里我们需要使用gulp-sass插件来帮我们完成，
安装方式非常简单，npm install gulp-sass --save-dev,

sass基本语法：
注释：支持/*注释*/和//注释2种方式
嵌套：
sass语法允许嵌套，比如：#main下有一个类为.header,那么我们可以写成一下形式：
#main{
    background:#ccc;
    .header{
        width:20px;
        height:20px;
    }
}
这样写起来更加直观，一看就知道.header是在#main下的

引用父选择器（&）：
有时候，在嵌套的子选择器中，需要使用父选择器，那么这时候可以通过&来表示，代码如下：
a{
    font-weight:bold;
    text-decoration:none;
    &":hover{
        color:#888;
    }
}

定义变量：
对于一些比较常用的值，我们可以通过变量储蓄起来，以后想要使用的时候就直接用就可以了，定义变量使用$符合，代码如下：
$lxl:980px;
#main{
    width:$lxl;
}

运算：
比如现在有一个容器总宽度是900，要在里面平均放3个盒子，那么我们可以通过变量来设置他们宽度，代码如下：
$lxl:900px;
.box{
    width:$lxl/3;
}

@import语法：
在css中@import只能导入css文件，而且对网站的性能有很大的影响，而sass中的@import则是完全实现了一套自己的机制，
他可以直接将指定文件的代码拷贝到导入的地方，代码如下：
例子：如我们导航条的css样式，我们在首页，付款，设置等页都需要导航条的样式，我们可以把导航条的css样式写到init.scss文件
我们需要直接@import "init.scss";就可以
@import "init.scss";

@extend语法：
有时候我们一个选择器中，可能会需要另外一个选择器的样式，那么我们就可以通过extend来直接将指定选择器的样式加入进来，
代码如下：
如下代码意思就是.serious-error选择器需要的样式.error选择器里的全部样式，所以我们
可以通过@extend .error;把.error选择器里的样式全部调用过来在.serious-error选择器里
.error{
    background-color:#fdd;
    border:1px solid #f00;
}
.serious-error{
    @extend .error;
    border.width:3px;
}

@mixin语法：
有时候一段样式代码，我们可能要用很多地方，那么我们可以把他定义成mixin，需要用的时候就直接引用就可以了，代码如下：
@mixin large-text{
    font:{
        famliy:Arial;
        size:20px;
        weight:bold;
    }
    color:#ff0000;
}
如果其他地方想要使用这个mixin的时候，可以通过@include来包含进来，代码如下：
.page-title{
    @include large-text
    padding:4px;
    margin-top:10px;
}
@mixin也可以使用参数，代码如下：
@mixin sexy-border($lxl,$lxl1){
    border:{
        color:$lxl;
        width:@lxl1;
        style:deshed;
    }
}
那么我们以后在include的时候，就需要传入参数了，代码如下：
p{
    @include sexy-border(bule,10px);
}
详情教程：
http://sass.bootcss.com/docs/sass-reference/